filter_objects(E)
SYNOPSIS
     object *filter_objects(object *arr, string fun [, mixed extra1, ...] )

DESCRIPTION:
     Similar to filter(), but calls arr[n]->fun([extra1, .... ]);

     If the call returns a value != 0, the object arr[n] ist included in
     the returned array.
     0-entries in arr are ignored.

SEE ALSO:
     filter(E),filter_indices(E),map_objects(E)

