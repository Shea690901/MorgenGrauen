SkillResTransfer()

FUNKTION:
     static void SkillResTransfer(mapping from_M, mapping to_M)

DEFINIERT IN:
     /std/living/combat.c

ARGUMENTE:
     from_M:	mapping mit zu kopierenden Werten
     to_M:	Zielmapping

BESCHREIBUNG:
     Interne Methode, die waherend der Ausfuehrung von Attack() durch
     Skills oder P_TMP_ATTACK_MOD geaenderte Werte selektiert in das
     Hauptangriffsmapping uebertraegt.

     Derzeit werden folgende Werte kopiert:
     SI_SKILLDAMAGE, SI_SKILLDAMAGE_MSG, SI_SKILLDAMAGE_MSG2,
     SI_SKILLDAMAGE_TYPE, SI_SPELL

SIEHE AUCH:
     P_TMP_ATTACK_MOD, UseSkill (Waffenfaehigkeiten)

26.Apr.2002 Gloinson@MG